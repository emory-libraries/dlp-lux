<% if can?(:read, document) && !document["child_works_for_lux_tesim"]  %>
  <%= render "uv", document: document %>
<% elsif document["has_model_ssim"] == ["Collection"] %>
<% end %>
<%= render "metadata_all", document: document %>
