<% content = capture do %>
  <% fields.each do |field| -%>
    <div class="row">
      <%= field %>
    </div>
  <% end -%>
<% end %>

<%= @tag.nil? ? content : tag.public_send(@tag, content, class: @classes, **@component_args) %>
